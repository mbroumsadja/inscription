{
  "name": "inscription",
  "version": "1.0.0",
  "type": "commonjs",
  "description": "inscription a été généré par Germin",
  "scripts": {
    "dev": "vite --host",
    "start": "live-server --port=3000 --host=localhost",
    "build": "node ./x/scripts/build.js",
    "preview": "vite preview",
    "deploy": "node ./x/scripts/deploy.js"
  },
  "devDependencies": {
    "gh-pages": "^6.0.0",
    "ora": "^5.4.1",
    "vite": "^6.3.5"
  },
  "dependencies": {
    "live-server": "^1.2.0"
  }
}
